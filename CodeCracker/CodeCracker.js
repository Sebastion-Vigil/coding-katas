
class BiCrypt {
  constructor() {
    this.plainTxt = {}, // ciphers keys, alpha chars vals
    this.cipherTxt = {} // alpha chars keys, ciphers vals
  }

  setEncryptionScheme (alpha, cipher) { // arrs
    // alpha => ciper chars keys, alpha chars values
    // ciper => alpha chars keys, ciper chars vals
  }

  encrypt () {
   // convert alpha txt to ciper txt
  }

  decrypt () {
  // convert ciper txt to alpha txt
  }

}
